#include <stdio.h>
#include <stdlib.h>

int main() {
    int valor,soma=0;
    scanf("%d",&valor);
    int tamanho = valor;
    for(int i=0;i<tamanho;i++){
        scanf("%d",&valor);
        soma+=valor;
    }
    printf("%d\n",soma);
    return 0;
}
